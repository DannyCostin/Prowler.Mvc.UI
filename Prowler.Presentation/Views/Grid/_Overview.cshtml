@using Prowler.Mvc.UI
@using Prowler.Presentation.Models
@model Prowler.Presentation.Models.MockProduct

<center><h2>Overview</h2></center>
<h3>Description</h3>

<h5>The ASP.NET MVC Grid lets the user choose options from a list of choices. It provides flexible data binding, api, events, appearance customization through templates, accessibility</h5>
<h5>The current demo of the UI for ASP.NET MVC Grid demonstrates an implementation of the most common scenarios in which the component is used.</h5>

<h3>DEMO</h3>
<div>
    @(Html.Prowler().Grid()
            .UniqueId(nameof(Product.Id))
            .BindTo(Model.ProductDataSource)
            .Height(450)
            .Columns(
                    i => i.BindTo(nameof(Product.Checked)).Width(50).AsEditable(GridInputType.CheckBox).HeaderAsCheckBox(),
                    i => i.BindTo(nameof(Product.Name)).Title("Name").Sorting("SortColumnName").Width(250),
                    i => i.BindTo(nameof(Product.Description)).Title("Description").Sorting("DescriptionFiltersSort"),
                    i => i.RowTemplate(@"<center><img src=""{#Image#}"" alt="""" style=""width:32px;height:32px;""/></center>", nameof(Product.Image)).Width(35)
                    )
            .Pagination(() => new Pagination().PageIndex(1)
                                          .PageItems(10)
                                          .Total(Model.TotalNrElements)
                                          .PaginationButtons(6)
                                          .Url("/Home/GridNavigateToPage"))
            .ActionSort("/Home/GridFilter")
            .Render()
 )
</div>

<h3>Description</h3>
<h5>The Grid is a component for displaying data in a tabular format and comes with a variety of features for data management, presentation, and customization.</h5>
<h5>The Grid can be bound to various data sources and comes with built-in common features, such as filtering, paging, sorting.</h5>
<h5>This demo shows a Data Grid with enabled paging, sorting, selection and cell templeting</h5>

<br/>

<h3>Functionality and Features</h3>
<ul>
    <li>Responsive and adaptive HTML5 rendering</li>
    <li>Built-in implementations for paging, sorting, filtering</li>
    <li>Column resizing</li>
    <li>Row checkbox selection</li>
    <li>Cell editing, templeting</li>
    <li>Header templeting</li>
    <li>Toolbar templeting</li>
</ul>